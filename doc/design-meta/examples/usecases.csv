priority,usecase,why_it_matters,minimum_library_support
1,parse supported keyIds with a schema,lets Dart code validate grammar without hardcoding path rules,parse into structured segments while traversing schema node definitions
2,get the parent or root of a key string,lets app code navigate raw keys like paths,provide parent and isRoot helpers for string inputs after schema validation
3,get parent or ancestors from a parsed key,lets app code avoid re-parsing when a ParsedKey is already available,provide ParsedKey-based parent and ancestor helpers
4,check descendant relationships within a list of keys,lets app code find related keys without building custom traversal code,provide descendant filtering with include-self and max-depth options for strings and ParsedKey values
5,configure grammar through schema data,lets the package adapt to allowed labels child ordering and value rules without parser rewrites,accept a normalized schema map keyed by label
6,keep canonical string form,lets app code compare and persist keys consistently,serialize parsed key back to canonical keyId
